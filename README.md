# Chess Annotator

Chess Annotator is a single-author tool for annotating your own games for a coach.

## Configuration

On Debian/Ubuntu, install the Cairo system library before running `pip install`:

```bash
sudo apt install libcairo2-dev
```
