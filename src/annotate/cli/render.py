"""chess-render — render an annotation to PDF (implemented in M3)."""

import sys


def main() -> None:
    """Run the placeholder ``chess-render`` CLI entry point.

    Rendering is not implemented yet, so the command prints a clear
    status message and exits with a non-zero status code to indicate
    that no document was produced.
    """

    print("chess-render: rendering not yet implemented (coming in M3).")
    sys.exit(1)
